// src/services/loyalty/config.js
//
// ⚠️ نفس فلسفة services/grouping/config.js بالضبط: هاد الملف مش مصدر
// الحقيقة وقت التشغيل - loyaltyService.js بيقرا القيم الحية من جدول
// system_settings (قابل للتعديل من لوحة الأدمن عبر GET/PUT /api/admin/settings
// الموجودة أصلًا). هاد الكائن هلق بس القيم الافتراضية لأول صف بالجدول.
module.exports = {
  LOYALTY_ENABLED: true,

  // كم نقطة يكسب الزبون لكل وحدة عملة (دينار) من قيمة الطلب النهائية
  // (final_amount) وقت التسليم. مثال: 1 = نقطة وحدة لكل دينار.
  POINTS_EARN_RATE: 1,

  // قيمة النقطة الوحدة بالعملة وقت الاستبدال بالـ checkout. مثال: 0.01 يعني
  // 100 نقطة = 1 دينار خصم - أعلى بكتير من معدل الكسب عن قصد (تشجيع تجميع
  // النقاط بدل استبدالها فورًا بنفس قيمتها، نمط قياسي بأي نظام نقاط ولاء).
  POINTS_REDEEM_RATE: 0.01
};
